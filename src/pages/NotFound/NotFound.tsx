export default function NotFound() {
  return (
    <section>
      <h1>Page not found</h1>
      <p>
        That route does not exist. Use the navigation above to get back into the app.
      </p>
    </section>
  )
}
